rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow public read for images, and write for authenticated users
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
